---
title: "Práctica 1. Aplicacion AR(1)"
author: "Tus datos"
date: "19/08/2026"
output: html_document
editor_options: 
  chunk_output_type: inline
---

### Dependencies and Setup
```{r setup, include = FALSE, warning = FALSE, message = FALSE}
# Ajusta lo que consideres relevante:
knitr::opts_chunk$set(echo = TRUE)

library(ggplot2)
library(dplyr)
library(readxl)
library(stats)

```

# Importamos Datos:

Los datos importados son:

* Pax_Nal: Pasajeros (Salidas) en vuelos nacionales
* Pax_Int: Pasajeros (Salidas) en vuelos internacionales 
* Vue_Nal: Vuelos u operaciones (Salidas) en vuelos nacionales
* Vue_Int: Vuelos u operaciones (Salidas) en vuelos internacionales
* Pax_Metro: Pasajeros transportados (Millones) en el SCM

Fuente: INEGI, https://www.inegi.org.mx/app/indicadores/?tm=0&t=1090

```{r}

Datos <- read_excel("Base_Transporte.xlsx", sheet = "Datos", col_names = TRUE)

head(Datos)

```

# Instrucciones:
A partir de este punto debes seleccionar una de las series en el dataframe.

A esa serie debes aplicar o implementar el código que se te solicita.

En algunas secciones se te pedirá que realices una breve explicación o interpretación de los resultados. Esas secciones de identifican con texto entre "[*]"

Recuerda que esta práctica está basada en lo visto en las clases de Introducción a R, Clase 01 (naturaleza de las series de tiempo) y Clase 03 (procesos AR(p) y FAC/FACP).

# Conversión a series de tiempo:

```{r}
# En esta sección coloca tu respuesta:
# En Niveles


# En Logaritmos:


# Diferencias mensuales:


```

# Graficas:

[Gráfica tu serie y describe brevemente el comportamiento; destacando algún elemento que te parezca relevante]

```{r, echo = FALSE}
# Coloca tu código de la gráfica de la serie


```

# Estimacion del proceo AR(1)
[A partir de alguna de las versiones de la serie (en niveles, logaritmo o diferencias) modela la serie como si fuera un AR(1). Discute brevemente tus resultados]


```{r}
# Coloca tu código de la serie seleccionada y modelada como AR(1)

```

# Conclusiones

[Discute brevemente tus principales resultados]


